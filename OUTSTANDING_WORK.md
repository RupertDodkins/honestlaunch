# CappinCheck Outstanding Work

This document is the implementation handoff for finishing CappinCheck after the initial scaffold.

Goal: execute the remaining work sequentially, self-verify each step, and keep the project demoable at every checkpoint. Do not remove the deterministic `--mock` path; it is the fallback for live demo failure.

## Current State

- Repo: `/Users/rupert/engineering/cappincheck`
- Initial commit: `993fdc2 Create initial hackathon scaffold`
- Working command:

```bash
source .venv/bin/activate
cappincheck audit examples/demo_document.md --mock --out examples/demo_report.md --json examples/demo_report.json --html examples/demo_report.html
open examples/demo_report.html
```

- Current architecture:
  - CLI-first Python package.
  - Static Markdown, JSON, and HTML report generation.
  - Role definitions in `skills/*/SKILL.md`.
  - Local async Gemini runner that loads the skill files.
  - Mock mode with deterministic numeric-stretch demo.
  - Real Gemini path requests Google Search, URL Context, and Code Execution tools.
  - Experimental managed runtime is available with `--runtime managed` via Google GenAI Interactions API.

## Completion Snapshot

- Done: live Gemini path verified on `examples/demo_document.md`.
- Done: malformed JSON repair path and CLI failure context added.
- Done: best-effort grounding metadata capture added for standard Gemini responses.
- Done: deterministic numeric calibration helper added and verified.
- Done: `--runtime managed` experimental Interactions path added and verified on one demo claim.
- Done: static dashboard filters, source links, and claim-strength UI added.
- Done: README/RUNTIME docs updated to describe local default and managed beta caveat.
- Remaining optional: choose a stronger real public source and pre-generate a stable report when network/API latency permits.
  - Managed Agents are documented as a future adapter boundary in `RUNTIME.md`; they are not the current runtime.

## Non-Negotiables

- Preserve `--mock` behavior throughout all changes.
- Keep the repo public-safe: no secrets, no private docs, no proprietary workflow examples.
- Do not add user accounts, auth, database, or a hosted backend.
- Prefer a reliable CLI + static report over a fragile app.
- Every step below must include a local verification command and a short note of the result.
- Keep README public-demo language accurate: local async Gemini runtime now, Managed Agents adapter later.

## Step 1: Verify Real Gemini Path

Objective: confirm that the live `google-genai` path works with the chosen model and tool config.

Tasks:

- Export `GEMINI_API_KEY`.
- Run a real audit on `examples/demo_document.md` without `--mock`.
- If `gemini-3.5-flash` rejects the current tool combination, identify the smallest model/config change that keeps the demo honest.
- If structured JSON plus tools are incompatible, add a repair/retry path rather than weakening the report schema.
- Record the working model name in the README.

Suggested commands:

```bash
source .venv/bin/activate
export GEMINI_API_KEY=...
cappincheck audit examples/demo_document.md --out examples/live_report.md --json examples/live_report.json --html examples/live_report.html --limit 3
```

Verification criteria:

- Command exits `0`.
- Markdown, JSON, and HTML files are generated.
- JSON validates as an `AuditReport`.
- At least one live claim audit includes evidence or missing-context content generated by Gemini.
- `--mock` still works after any changes.

Fallback rule:

- If the newest model path is unavailable, prefer a documented environment variable override such as `CLAIMLENS_MODEL=<working-model>` over hardcoding a less relevant model.

## Step 2: Add JSON Repair And Error Reporting

Objective: make live runs robust when Gemini returns malformed or partially invalid JSON.

Tasks:

- Wrap `GeminiClient.structured(...)` with one retry path.
- On parse/validation failure, send the raw model output back to Gemini with a strict "repair this to the schema" instruction.
- If repair fails, raise an error that includes:
  - model name
  - schema name
  - first 1,000 characters of raw output
  - suggested next command using `--mock`
- Avoid silent fallback from live mode to mock mode.

Verification criteria:

- Existing mock command still exits `0`.
- A small unit-style script or command can feed malformed JSON into the repair helper and produce valid schema output, or cleanly report failure if no API key is present.
- Real audit failures are debuggable from the CLI output.

Suggested verification:

```bash
source .venv/bin/activate
python -m compileall cappincheck
cappincheck audit examples/demo_document.md --mock --out examples/demo_report.md --json examples/demo_report.json --html examples/demo_report.html
```

## Step 3: Improve Grounded Citation Capture

Objective: make source evidence visible and judge-legible in reports.

Tasks:

- Inspect the `google-genai` response shape for grounding metadata.
- Capture grounding URLs/snippets when available.
- Merge those citations into `EvidenceItem` records or add a report-level `sources` section if the metadata is not naturally claim-specific.
- Render source links in Markdown and HTML.
- Keep schema backward-compatible with existing generated reports if possible.

Verification criteria:

- Real audit output contains clickable source URLs when Google grounding returns them.
- HTML dashboard renders source URLs without layout breakage.
- JSON report includes source metadata.
- Mock reports remain deterministic and still render.

## Step 4: Add Deterministic Numeric Calibration

Objective: make the numeric-stretch demo reliable even when model/code execution is uneven.

Tasks:

- Add a small local numeric helper for percent and percentage-point claims.
- Detect simple patterns such as:
  - `baseline = 84.1`, `method = 87.3`
  - table rows with percentages
  - text containing "improves by X%"
- Compute:
  - absolute point gain
  - relative percent gain
  - mismatch between claimed and computed improvement
- Feed deterministic numeric findings into the numeric calibrator or aggregator.
- Do not turn this into broad regex-based language understanding; keep it limited to stable numeric table/text extraction.

Verification criteria:

- `examples/demo_document.md` produces the 84.1 -> 87.3 calculation in both mock and live-capable paths.
- JSON report includes numeric findings.
- HTML dashboard shows numeric findings for the selected claim.
- The helper has at least one direct command or lightweight test that checks the 84.1/87.3 case.

## Step 5: Add Managed Agents Adapter Or Explicit Runtime Boundary

Objective: make the repo honestly native to the Google I/O Managed Agents story while preserving reliability.

Tasks:

- Search official docs or local SDK capabilities for Managed Agents / Interactions API availability.
- If callable in the current environment, add a `--runtime managed` option that dispatches each skill as a Managed Agent.
- Keep `--runtime local` as default until managed runtime is verified.
- If Managed Agents cannot be called reliably, add a clear `RUNTIME.md` explaining:
  - skill files are runtime-portable agent definitions
  - local runner is the fallback
  - where the Managed Agents adapter should plug in
  - exact current blocker
  - README must not claim Managed Agents are currently executing audits.

Verification criteria:

- `cappincheck audit ... --runtime local` works.
- `cappincheck audit ... --runtime managed` works on the demo document.
- If not implemented, `RUNTIME.md` is specific enough that another agent can add the adapter without rediscovering the architecture.
- README does not overclaim Managed Agents execution if only local runtime is available.

## Step 6: Add A Real Public Demo Document

Objective: replace the synthetic-only demo with a real public artifact that demonstrates grounded claim auditing.

Tasks:

- Choose one public AI/science paper, model report, benchmark post, or technical blog with bold but checkable claims.
- Add only public-safe source references and generated reports.
- Prefer a document with one numeric, benchmark, generalization, or novelty stretch.
- Do not include copyrighted full paper text if licensing is unclear; store a URL and generated report instead.
- Keep `examples/demo_document.md` as a deterministic fallback fixture.

Verification criteria:

- A live or pre-generated report exists for the real source.
- The report has at least one memorable claim row with:
  - original claim
  - verdict
  - cap score
  - weaker supported rewrite
  - source evidence or missing context
- README points to both:
  - deterministic mock fixture
  - real public example
  - current placeholder: `examples/real_public_example.md`

## Step 7: Dashboard Polish

Objective: make the static report demo-ready without adding backend complexity.

Tasks:

- Add basic filtering by verdict/vibe.
- Improve the claim strength visualization.
- Ensure long claims wrap cleanly.
- Ensure source URLs are clickable.
- Keep all report data embedded in the static HTML.

Verification criteria:

- Open `examples/demo_report.html` locally.
- Click through each claim.
- Filter by at least one verdict/vibe.
- Resize to a narrow viewport and confirm no major overlap.
- No network request is required to view the report.

## Step 8: Public Repo Readiness

Objective: make the repository ready to push public.

Tasks:

- Run a final secret scan.
- Confirm `.gitignore` excludes `.env`, `.venv`, caches, and build outputs.
- Add screenshots only if they are public-safe and helpful.
- Add a short demo script section to README.
- Add a "Limitations" section that says CappinCheck is triage, not scientific proof.

Suggested commands:

```bash
rg -n "AIza[0-9A-Za-z_-]+|gho_[0-9A-Za-z_]+" . --glob '!/.env' --glob '!/.venv/**' --glob '!/.git/**'
git status --short
```

Verification criteria:

- No real secrets are present.
- README renders the core pitch clearly.
- Fresh clone instructions are sufficient.
- Repo can be pushed public without cleanup.

## Step 9: Final Demo Rehearsal

Objective: ensure the live hackathon path has a fallback.

Tasks:

- Run the live command once if API access is available.
- Run the mock command once.
- Open the generated HTML dashboard.
- Prepare a 90-second demo script.
- Prepare a backup pre-generated report and, if time allows, a screen recording.

Demo command:

```bash
cappincheck audit examples/demo_document.md --mock --out examples/demo_report.md --json examples/demo_report.json --html examples/demo_report.html
open examples/demo_report.html
```

Verification criteria:

- Live or mock demo completes in under 90 seconds.
- Dashboard opens locally.
- One claim clearly shows a stretch caught by numeric calibration or grounded evidence.
- The demo sentence is ready:

> Before Gemini 3.5 Flash and Managed Agents, running multiple grounded subagents in parallel sandboxes, each browsing the web, executing code, and grading a single claim, would have cost dollars and minutes per audit. Today it costs cents and seconds, which is what makes a live claim ledger possible.
